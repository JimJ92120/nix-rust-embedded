[package]
name = "aarch64-none"
version = "0.1.0"
edition = "2024"

[dependencies]

[profile.dev]
panic = "abort"
lto = true
opt-level = "s"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
